# 王手中の反則経済 P0（issue #31）

**位置づけ**: 王手中の反則（アリーナ実測で全反則の 44.8%）が「知らない」せいなのか
「知っていて払っている」せいなのかを、**runtime を一切変えずに**測る。
issue #28（詰み経済）の総合判定「受けは検出できるが、受けた後の継続が反則負けで終わる」の
続きにあたる。

計測は P0-1〜P0-7 の7段。この文書は**実装済みの段**と、その読み方・落とし穴を書く。

| 段 | 何を識別するか | 実装 | 状態 |
| --- | --- | --- | --- |
| P0-1 型の分類 | どの型がどれだけあるか（分母つき） | `bin/analyze`（粒子不要） | 実装済み |
| P0-2 手種別・順番別の較正 | H3（較正） | `bin/analyze`（粒子不要） | 実装済み |
| P0-3 ソルバー方策の破滅の原因 | 「ソルバーに従え」の危険 | `bin/analyze`（粒子不要） | 実装済み |
| P0-4 w\* 監査 | H1 の感度・H2 の材料 | `bin/check_probe`（粒子を回す） | 実装済み・**計測済み** |
| P0-5 方策シミュレーション | H1 / H2 の介入 | `bin/check_policy`（粒子を回す） | 実装済み・**計測済み** |
| P0-6 継続の局所効果 | 勝敗への変換（検証セット） | `bin/check_continue`（継続対局を回す） | 実装済み・**主 arm `alpha@k2` で検証セット待ち** |
| P0-7 反則トークンの影の価格 | H1（主測定） | `bin/check_price`（継続対局を回す） | 実装済み・**計測済み** |

**γ（H3 の較正マップ）の枝は P0-2 で落ちている**ので、P0-5 に γ の arm は無い
（下記「H3（較正）は棄却」）。残るのは α（価格）と β（動的継続価値）。

**2026-08-28 の P0-5 / P0-7 で枝が絞れた**（下記「P0 の判定」）:
**α は P1 へ進む**（反則 −0.23〜−0.41・破滅は改善・反則0 の手番は無害）、
**β は中止**（近似通過条件が両相手で不通過・正確版のコストは門の約300倍）、
**γ は引き続き指示されない**（ただし「符号すら逆」は候補分布では成り立たないので訂正）。

## P0-4 w\* 監査（`bin/check_probe`）

「浮かせたい候補がそもそも在るのか」を方策を変える前に測る
（`TSUITATE_PROBE_AUDIT` と同じ考え方）。**runtime には何も入らない**。

決定点は「王手中で実際に反則した bot の手番」で、既定の対象は型1
（玉以外 → 玉の手）と型2（玉以外 → 玉以外）。1決定点につき2つのランキングを取る:

1. **手番開始時（反則0）** … `for_each_decision_full` が渡すのはその手番の反則を
   全部食った後の状態なので、両者のログ末尾から反則の観測を `fouls_this_turn` 本
   だけ落として復元する（`add_foul_obs` が対で積む `MyFoul` / `OpponentFoul`。
   落とす末尾が本当に反則の観測かを確かめてから落とす）
2. **実戦の反則列を食った後（実再決定）** … `Estimator::update` を通した本物の
   再ランキング。若返り・ソフト救済・`prior_weight` の反則比例ブースト・
   `foul_tried` の除外まで全部通る。**prewarm は1回だけ**で、①の instance を
   `clone_boxed` して `MyFoul` を食わせた**継続**として作る（別々に組み直すと
   壁時計デッドラインのぶん粒子集合が別物になり、反則注入の効果と再構築ノイズが
   混ざる。下記「H2 は対照との差で見る」）
3. **対照（A）** … ②と**同じ視界（＝同じ残り反則＝同じ価格）・同じ除外集合**で、
   **反則の観測だけを入れずに**選び直したランキング

出す量:

- **k\***: 玉の手が首位になる最小の反則コスト倍率。価格は玉の手の (1−p) 側にも
  効くので「score 差 ÷ プローブの傾斜」では出ない。**P1-α と同じ
  `check_cost = max(k × base, 床)` で全候補を再計算して交点を探す**
  （`check_economy::k_star`。`score = min(p·G,G) − (1−p)·c + …` なので
  コストの付け替えは `−(1−p)(c_k − c)` で厳密。gain も p_legal も再計算しない）。
  順位は**乱数を除いたスコア**（`score − tiebreak`）で付け、同点は USI の辞書順
- **門の分母は「首位が玉以外」の決定点だけ**。決定点の分類は seed の多数決で
  1回だけ決め、多数決で既に玉の手なら門の分母から**丸ごと**外す
  （少数側の 1 unit だけで分母1の「反転」を数えると門の割合が水増しされる）
- **反則注入後の実再決定が静的な次点と違うか**（H2 の生の値）。「静的な次点」＝
  初回ランキングから実戦の反則列を除いた最上位 = 現行 `combine_score` が暗黙に置く
  「次善手固定」の仮定そのもの
- **H2 は対照との差で見る**: 生の「静的な次点と違う」率は**再決定そのもののノイズ**を
  含む（同じ設定の2 run で初回の首位が 67〜69% しか一致しない = 壁時計予算で
  粒子数が揺れるため）。対照（A）は価格も除外集合も B と同じで観測だけが無いので、
  **B と A の差が「反則を観測したことによる信念更新の効果」そのもの**。
  A と静的な次点の差はノイズ床として併記する
- 監査の健全性: **初回ランキングの首位が実戦の最初の反則と一致した率**
  （オフライン復元がその決定をどれだけ再現できているか。予算やコードが記録と
  違うと下がるので、判定の前にここを見る）

中止条件: **k\* ≤ 3 で反転する決定点が 20% 未満 かつ 反則注入後に首位が変わる
決定点も 20% 未満**なら α / β の枝は中止（m061/m036 型の「候補が全部悪手」）。

seed は独立標本として数えず、決定点ごとに多数決を取る。

## P0-5 方策シミュレーション（`bin/check_policy`）

王手中の1手番を「候補 × p_legal × 価格」の小さな世界として取り出し、方策を
**反則するたび更新しながら真実で裁定**する。**runtime には何も入らない**。

estimand は2つ（P0-6 の門と同じ割り方）:

- `foul` … 実戦で反則した王手手番（改善側）
- `nofoul` … 反則0で受理された王手手番から**同数**を決定論的に抽出した対照
  （新しいプローブを足す害はここで見る。現行方策の順位で選ぶと対照が方策に
  引きずられるので `(局, 手数)` の辞書順で等間隔に採る。**全記録を見てから
  割る**ので、シャードが違っても同じ標本になる）

arm は `<方策>@<更新規則>` の形:

| 方策 | 内容 |
| --- | --- |
| `current` | 現行 `combine_score` |
| `alpha@k1.5` / `@k2` / `@k3` | 反則価格 ×k（`c = max(k × base, 床)`。**床は倍率化しない**） |
| `beta@k1l0.5` / `@k1l1` | `c_eff = max(0.5c, c − λ·ΔV)`、**残り1回では無効** |
| `beta_order@l0.5` / `@l1` | 現行がプローブを選ぶ決定に限りプローブ同士だけ並べ替え |
| `solver_greedy` | 解消確率の argmax（参考。P0-3 の破滅を継承する） |

更新規則は issue の「4本の対照」に対応する:

1. 記録された実反則列 … 出力の `record_fouls`（再現の基準）
2. `current@static` … 初回ランキングの次点を取る静的方策
   （= 現行 `combine_score` が暗黙に置く「次善手固定」の仮定**そのもの**）
3. `current@real` … **実再決定**（`MyFoul` を注入して `Estimator::update` と
   ランキングを走らせる。正解基準・呼び出しごとに思考予算をまるごと使う。
   gain も `removal_term` も作り直されるので**候補リストごと差し替える**）
4. それ以外 … **p-only shadow update**（snapshot ベースの仮想更新）

### p-only shadow update は `evaluate` と同じ式を呼ぶ

仮想更新（`check_policy::ShadowUpdater`）がやるのは2つだけ:

- **m が合法だった粒子を落とす**（合法性は物理制約なので厳密粒子にも taint にも効く）
- **`CheckSolver` をその手番の反則列込みで作り直す**（= `observe_foul`）

そのうえで p_legal を `strategy::blend_p_legal`（**`evaluate` が呼ぶのと同じ関数**）で
組み直す。gain・リスク・`removal_term` は再計算しない。ブレンドを別々に書くと
「仮想更新が実再決定とずれた」のか「式が食い違っていた」のかを分けられないので、
`evaluate` から関数を切り出して共有してある（挙動は不変）。

**健全性の検査**: 反則0で仮想更新を掛けると初回ランキングの p_legal を
**厳密に再現する**はず（ローカル実測で最大差 0.0000）。ここがずれていたら
`evaluate` の p_legal 経路（`known_covered_king_move_cap` などの min 系）を
取りこぼしているので、**判定の前にこの行を見る**。

### ΔV は水準の差を引く（β が α の役割を奪わないように）

`ΔV(m) = Vpost(m) − Vpre(m)` は「m を除いた次候補の価値」の、反則を観測した
前後の差。生の差には **m に依らない定数**（反則を1つ払えば価格は全候補で上がり
p も全体に動く）が乗るので、issue の「同じ参照候補を引いた候補間 advantage」を
**参照＝候補平均**で実現する。こうすると平均 ΔV が 0 になり、β は価格の
**水準**を動かさず候補間の**配分**だけを変える（＝ α と役割が分離する）。

計算量は「首位から `c` 以内の候補だけ仮想更新する」で抑える。β が動かせる score は
高々 `(1−p)·(c − c_min) ≤ 0.5c` なので、それ以上離れた候補は β では首位に
なれない（近似ではなく上界による厳密な枝刈り）。

### 近似通過条件（事前登録）

β の対現行改善量が**正のときだけ**比率を定義し、`current@shadow` と
`current@real` の「受理までの反則数/手番」の差がその **25% 以下**なら通過
（＝ P1-β の事前スコアを snapshot で作ってよい）。改善量が 0 以下なら β は不採用。
超えるなら P1-β は**候補ごとに推定器を clone して仮想 `MyFoul` を注入する**設計に
変えることになり、そのコストを p95 で払えるかを別に測る必要がある。

### 主指標は真実ベース（受理手の gain は補助）

受理直後の **材料差・被一手詰め・次の被王手**を真実局面から数える。受理手の
現行 gain を主指標にすると、β が評価の穴を突いたときに自己正当化する。
破滅率（10回到達・≥8・p95）も必ず併記する（P0-3 の「ソルバーに従え」は解ではない）。

**候補分布上の較正**も出す: P0-2 の較正は「現行方策が選んだ手」のものなので、
順位を変えた後の候補分布へは外挿できない。初回ランキングの全候補を真実で
ラベル付けして手種ごとに出す。

## P0-6 継続の局所効果（`bin/check_continue`）

王手中の1手番を方策どおりに指させ（非合法なら実対局と同じく反則を積んで次候補へ）、
そのあとを通常方策で終局まで指し継いで 勝1 / 分0.5 / 負0 で数える。
P0-5 が出すのは「反則が減ったか・真実指標が良くなったか」で、**勝敗への変換**は
ここでしか測れない。

**これは一手番の局所効果であって、全局反復適用の下界でも上界でもない**
（反復すれば節約が将来にも効いて大きくなることも、状態分布が変わって逆転する
こともある）。P1 の arena へ進むための局所的な有効性確認と位置づける。

- **検証セットでだけ回す**: 発見セット（P0-1〜P0-5 / P0-7 を回した記録）で回すと、
  主 arm を選んだのと同じデータで検証することになる。**未使用の `match_seed`**
  で取り直した Arena 実行を `arena_run_id` に指定した**別の起動**で回し、
  発見セットを見てから水準を変えない
- **1元対局につき estimand ごとに最大1決定点**（同じ元対局の相互排他的な未来を
  足し合わせない）。候補が複数あるときは最初の対象手番
- 強制列は P0-5 と**同じ配管**（`entry_setup` → `simulate`）で作る。`simulate` の
  `sequence` は「反則した手…受理された手」なので、そのまま `force_move` へ渡せば
  実対局と同じ裁定（反則は手番を変えない・上限10回で反則負け）を再現する
- **継続の乱数は arm に依らず `(局, 決定点, seed)` だけ**から作る（共通乱数）。
  同じ強制列になった arm は継続を1回だけ走らせて結果を配る
- Δ の分母は**全対局数**（対象の手番が無かった局は 0）。CI は元対局単位の
  cluster bootstrap。**シャードが欠けたら判定を出さない**
- 比較の基準は `baseline`（実戦の反則列＋受理手）ではなく **`current`**
  （同じ配管でオフラインに引き直した現行方策）。オフラインの再決定は実戦の選択と
  64〜67% しか一致しない（P0-4）ので、baseline と比べると「指し直したから」の差が
  Δ に混ざる。baseline は**その混入量の目安**として併記する

門（issue #31 で事前登録）:

- 反則あり estimand: 主 arm の **`Δpolicy − Δcurrent ≥ +0.04`（CI 下限 > 0）**
  かつ foul_limit・破滅率が悪化しない
- 反則0 estimand: **非劣性**（`≥ −0.01` かつ CI 下限 > −0.02）かつ即時反則・
  破滅率・foul_limit が悪化しない。ここで改善は要求しない
- **β-order は反則経済施策なので即時反則の非増加を必須**にする。
  **full β は反則増を許し**、勝率改善と foul_limit 非悪化で判定する

## P0-7 反則トークンの影の価格（`bin/check_price`）

王手手番の**直後**（受理手を指した後）の盤面・信念・乱数を固定し、
**審判の累積反則数だけを +1 した** paired continuation を共通乱数で走らせる。
スコア差 = 「反則1個の controlled direct effect」＝ 情報価値を固定した純粋な
トークン価格。裁定は `selfplay::play_continuation`（通常アリーナと同じ関数・時計は無効）。

- **単位が違う**: 影の価格は勝率単位、`foul_cost` は評価点単位。比べられるのは
  **残数に対する曲線の形・比率**だけで、「水準が過小」はここでは判定しない
- **ログには反則を足さない**（足すと相手の信念と自分の `foul_tried` まで動いて
  「情報価値を固定した」ことにならない）。動かすのは審判の累積数
  （`StartState.fouls`）と自分の視界（`PlayerView.fouls`）だけ。
  **継続側の全コードが累積数をそこからしか読まない**ことは
  `selfplay` のテスト `継続の累積反則数は開始状態から読む` で固定してある
  （ログに `MyFoul` が1つも無い状態で 9 反則から始めて、1つ積んだら反則負けになること・
  戦略の視界にもその累計が見えていることの2点）
- **残り1を0にする treatment は継続を始めず即 foul_limit 負け**（0 点）
- **1局から採る決定点は `--per-game` 本まで**（同じ局の相互排他的な未来を
  足し合わせない）。残り反則の水準が散るように累計反則の順で等間隔に採る
- 層別は 残り反則 × 手数帯 × **相手の残り反則**（効果修飾）。CI は元対局単位の
  cluster bootstrap（seed も決定点も独立標本として数えない）

## 実装済みの3段（粒子を回さない）

入力は**アリーナの対局記録**（`arena-records-*` の JSONL）。真実の全手順・反則試行・
選択手の `debug.p_legal` が入っているので、粒子を回さずに全部復元できる。
共有定義は `src/check_economy.rs`（`bin/analyze` と、以後の P0-4/P0-5/P0-7 が
**同じ定義**で手番・手種・順番を数えるための場所。別々に数えると較正の数字が意味を失う）。

### 視界の復元は決定点の手数を渡す

`check_economy::view_from_model` に**必ず決定点の手数**を渡す（`scenario_core::make_view`
と同じ規約）。`CheckSolver` は既知の敵駒マスの鮮度を `view.move_number − 8` で切るので、
0 を渡すと窓が無制限になり、実戦では載らない古い幻の駒を base に載せた**別物の
ソルバー**になる。ここがずれると手種の分類（`captures_checker`）・ソルバー p の順位・
仮説のシェアとエントロピー・破滅の判定が全部ずれる。
（初版はここを 0 のまま移設していて、ローカル記録で「破滅 7手番 / ソルバー最善が
最初から合法 38.9%」と出ていた値が修正後は「3手番 / 36.3%」に変わった。
run 33141692604 の**ソルバー由来の列は撤回**し、修正後に取り直す。
記録の `p_legal` から作る P0-2 の較正と、粗い束（玉 / 玉以外 / 打ち）で決まる
型の表はこの影響を受けない）

### 手種は「bot の意図」で分ける

`CheckMoveKind` は真実の駒配置ではなく **`CheckSolver::captures_checker`**
（王手駒仮説のマスへ玉以外で移動し、その仮説の下で解消する手）で捕獲試みを判定する。
空振りの捕獲試みも意図としては捕獲プローブなので、同じ束に入れないと
「プローブの較正」が測れない。真実で敵駒がいたか・それが王手駒かは
**別の列**（`truth_enemy_at_to` / `truth_checker_at_to`）に持つ。

### 分母は反則0の手番も含む全王手手番

型の表の割合は**王手中の bot の手番全体**を分母にする。反則した手番だけを数えると、
P0-6 の非劣性 estimand（新しいプローブを足す害）に対応する母数が見えない。

### 手数の対応

`check_economy::decision_groups` に閉じる: 反則は手番を変えないので `MyFoul` の
`move_number` はその決定点の手数そのまま、`MyMove` は**適用後**の値なので 1 引く。
受理手が出たらそこで手番を閉じる（同じ手数の手番が2つ続くことはない）。
`accepted_legal_at_entry` は**整合性検査**（反則は局面を変えないので常に true。
false が出るなら手数の対応がずれている）で、発見ではない。

### 「事後的には無駄だった」を測る列

- `legal_candidates_at_entry` … 開始時点で**真に合法だった候補の本数**。
  1本しか無い手番の確かめは価格では避けられない
- `best_legal_rank_at_entry` … 真に合法な候補のうちソルバー p が最上位のものの順位。
  1 なら「ソルバー最善が最初から合法だったのに反則してから到達した」。
  **分母は開始時にソルバーを作れた手番だけ**（両王手で仮説が全滅するとソルバーが
  作れず順位が付かない。0 として数えると率が下振れするので判定不能として別に出す）
- `accepted_rank_at_entry` … 受理された非玉手の開始時ソルバー p 順位。
  **エンジンの順位ではない**（粒子が要るので P0-4 の領分）。上位なら
  「情報が生産的だった」のではなく「単に次点へ進んだだけ」の可能性が高い、という粗い代理
- `true_hyp_share` / `hyp_entropy` … 真の王手駒（マス・駒種とも一致）に載っている
  仮説の重みシェアと、仮説重みの正規化エントロピー。**「知らない（希釈）」と
  「知っていて払う」を分ける量**

### P0-2 の判定は Brier ではない

H3 の門は**方向つき較正誤差**「捕獲試みの 平均予測 − 合法率」の
**元対局 cluster-bootstrap CI の下限が 0.1 を上回る**こと、かつ v13 / v14 の
**両相手で同方向**であること。Brier は H1（価格）と H3（較正）を分けない。
`cluster_ratio_ci` の統計単位は元対局で、手番も seed も独立標本として数えない。

この較正は**現行方策が選んだ手**のものであって、γ で順位を変えた後の候補分布へは
外挿できない（候補分布上の較正は P0-5 で真実ラベル付けして取る）。

### P0-3 は「ソルバーに従え」の反証を残す

解消確率の argmax で指し直すと反則は減る（実測 291 → 211）が、**8手番は逆に
8〜10反則**を積む。P1 の方策がソルバーの p を強く信じるほどこの破滅を継承するので、
破滅手番（8反則以上）ごとに ①真の王手駒が仮説集合にあったか（マス・駒種とも一致 /
マスのみ / 無い）②積んだ反則の原因（真実から分類: 王手駒が残る / 捕獲先が支えられて
いた / 別の敵駒の利き / 経路 / 打ちマス）を記録する。
`CheckSolver::legal_under` が仮説の王手駒1枚しか置かない盲点（支え駒・ピン）は
**この issue では直さない**（原因を記録するだけで別 issue）。

## 回し方

**通常のコード push では走らない**。既存の Arena 実行の記録に対して回す:

```
gh workflow run check-economy.yml -f arena_run_id=<Arena実行ID> \
  -f opponents="estimator_v13 estimator_v14"
```

`gh` が使えないときは `.github/ci/check-economy.request.json` を置いて push
（例は `check-economy.request.example.json`。削除の push は全ジョブがスキップされる）。

**相手ごとに別ジョブで集計する**のが要点: 判定は「v13 / v14 の両相手で同方向」なので、
両者の記録を混ぜて1つの数字にしてはいけない。

同じ集計行は `arena.yml` のジョブサマリーにも出る（**analyze に集計行を足したら
arena.yml と check-economy.yml の両方のフィルタを直すこと**）。

**重い段は既定では走らない**。要る段だけ true にすること（全部同時に有効化すると
相手2 × シャードで 30 ジョブを超えてキューが詰まる）:

| 段 | 有効化 | 分割 | 相場 |
| --- | --- | --- | --- |
| P0-4 `bin/check_probe` | `run_probe` | `probe_shards`（既定4） | 粒子を回す |
| P0-5 `bin/check_policy` | `run_policy` | `policy_shards`（既定6） | 粒子を回す。決定点が P0-4 の約4倍（反則0の対照が同数入る × 4 seed） |
| P0-6 `bin/check_continue` | `run_continue` | `continue_shards`（既定8） | 継続対局を回す。**検証セットを指す別の起動で**（`continue_policy` に主 arm を1本） |
| P0-7 `bin/check_price` | `run_price` | `price_shards`（既定8） | **継続対局**を回すので最も重い。予算は 700ms が既定 |

どれも元対局単位で割り、`*-aggregate` が `report` で合算する。
**シャードが欠けたら失敗させる**（決定点の分母が狂うため）。CSV の1行目の
`#meta` に seed 数・型・予算・config 指紋・コード版（`source_fingerprint`）・
記録集合の指紋・**相手**・決定点数を書き、`report` が「同じ実験の独立サンプルか」を
検査する（別の実験を混ぜた集計・重複シャードは失敗）。

完全性の検査は3段で、**どれも既定では失敗**（`--allow-incomplete` で警告へ降格）:

1. **決定点ごとに seed が揃っているか**（揃わないと多数決の分母が決定点ごとに変わり、
   2/3 seed の点は同数で「反転しない」側へ倒れる）
2. **決定点そのものが消えていないか**（全 seed でランキングが取れなかった決定点は
   行が1つも残らないので、行がある決定点しか見ない ①では捕まらない）
3. **H2 の値（生 / 対照 / 正味）が欠測していないか**（B や A のランキングだけ
   取れなかった unit は行が残って `changed` 系が空になり、集計の `filter_map` が
   黙って落とす。全 seed で欠測した決定点は H2 の分母から丸ごと消えるので、
   20% の門が欠測の偏りで動く）

数値列（`k_star` と p_legal 系）は**構文だけでなく値域も検査する**:
`k_star` は有限かつ 1 以上、`p_king_after` / `p_king_ctrl` は空または有限な
[0, 1]。`p_king` だけは NaN が「その決定点に玉の手が無い」正規の sentinel なので
許すが、それ以外の非有限値と範囲外は拒否する（通すと集計の「p_legal の変化」が
黙って NaN になる）。

**相手を meta に持つ**のは、`records` の指紋がディレクトリ全体のハッシュなので
**同じ記録集合から `--opponent` だけ変えた2本を指紋では区別できない**ため。

思考予算は**記録を取ったアリーナと揃える**こと
（予算が違うと粒子数が変わり、「その決定点で実際に見えていたランキング」では
なくなる）。ローカルなら:

```
TSUITATE_THINK_BUDGET_MS=2000 cargo run --release --bin check_probe -- \
  --seeds 3 --jobs 6 --out data/check_probe.csv <records...>
TSUITATE_THINK_BUDGET_MS=2000 cargo run --release --bin check_policy -- \
  --seeds 4 --jobs 6 --out data/check_policy.jsonl <records...>
TSUITATE_THINK_BUDGET_MS=700 cargo run --release --bin check_price -- \
  --seeds 2 --per-game 2 --jobs 6 --out data/check_price.jsonl <records...>
```

**P0-7 だけは継続対局なので予算が違う**（`bin/mate_continue` と同じ 700ms）。
記録を取ったアリーナと揃えるべきなのは「その決定点のランキングを復元する」段
（P0-4 / P0-5）で、P0-7 が復元するのは**局面と累積反則だけ**なので、
継続の予算は control / treatment で同じでさえあればよい。

## 実測（発見セット: run 32697854659 の main vs v13 / v14 各104局）

**run 33149098094**（2026-08-28。`view_from_model` の手数修正・門の分母の取り方・
判定不能バケットまで入れた確定値）。v14 側は **issue 本文の表を完全に再現する**
（王手中 178手番 / 291反則、型1 = 61手番 118反則、残り反則の分布 10:29 9:36 8:26
7:18 6:14 5:15 4:15 3:8 2:7 1:10）ので、`bin/analyze` の復元は実戦の分類と一致している。

| | vs v14 | vs v13 |
| --- | ---: | ---: |
| 王手中の手番（分母） | 559 | 515 |
| うち反則あり / 反則数 | 178 / 291 | 139 / 252 |
| 型1 玉以外の盤上手 → 玉の手 | 61手番 (10.9%) / 118反則 | 39 (7.6%) / 79 |
| 型2 玉以外 → 玉以外（kakutori 型） | 50 (8.9%) / 73 | 40 (7.8%) / 67 |
| 型3 玉の手 → 玉の手（逃げ先の選別） | 43 (7.7%) / 58 | 33 (6.4%) / 57 |
| 型4 玉の手 → 玉以外 | 7 (1.3%) / 12 | 3 (0.6%) / 10 |
| 打ちで反則 | 2 / 3 | 1 / 2 |
| 受理手なし（その手番で終局） | 15 / 27 | 23 / 37 |
| 反則0で受理 | 381 (68.2%) | 376 (73.0%) |
| 最初の反則の着手先（真実）: 王手駒 / 他の敵駒 / 空 | 28 / 9 / 141 | 17 / 3 / 119 |
| 開始時に真に合法な候補 1本以下 / 2〜3本 / 4本以上 | 52 / 78 / 48 | 21 / 73 / 45 |
| ソルバー最善が最初から合法だった | 84/178 (47.2%) | 75/139 (54.0%) |
| 受理された非玉手の開始時ソルバー p 順位 1位 / 2〜3位 / 4位以上 | 1 / 10 / 46 | 0 / 2 / 41 |
| 真の王手駒の仮説の重みシェア（反則あり / 反則0） | 0.037 / 0.078 | 0.035 / 0.084 |
| 仮説の正規化エントロピー（反則あり / 反則0） | 0.993 / 0.975 | 0.994 / 0.981 |
| **捕獲試みの方向つき較正誤差** | **+0.025 [−0.008, +0.057]** | **−0.009 [−0.055, +0.036]** |
| ソルバー方策の破滅（8反則以上） | 4手番 | 2手番 |

### H3（較正）は棄却

門は「捕獲試みの 平均予測 − 合法率 の元対局 cluster CI 下限 > 0.1 かつ両相手で同方向」。
実測は **v14 +0.025 / v13 −0.009 で符号すら逆**、CI 下限はどちらも 0 未満。
**捕獲プローブの p_legal は概ね当たっている**（v14 の順番別: 0回目 +0.041 /
1回目 −0.004 / 2回目以降 −0.028、v13 は 0回目 −0.051 / 1回目 +0.076 /
2回目以降 +0.102 と順番による向きも一致しない）。**P1-γ は指示されない**。

過信が出ているのは**玉の手**のほう（v14 順番0 +0.048 / 順番2+ **+0.096**、
v13 順番0 +0.058 / 順番2+ **+0.126**）で、これは `CheckSolver::legal_under` が
仮説の王手駒1枚しか置かない既知の盲点（支え駒・見えない利き）と同じ方向。
ただし γ の設計は**玉の手には掛けない**（一律の玉割引が3シード全て悪化した
`check_king_prior` 第1版の教訓）ので、この過信はそのままでは補正に使えない。
仮説側（`legal_under` の拡張）は別 issue。

### 破滅は「知らない」ではなく「希釈」

ソルバー方策が 8反則以上を積む手番は 6（v14 4 + v13 2）で、**そのすべてで
真の王手駒はマス・駒種とも一致する仮説として集合に載っている**。積んだ反則の原因も
「王手駒が残る（仮説違い）」が 37 / 13 で支配的 = **正しい仮説が一様な仮説群に
埋もれて argmax が別の手を選ぶ**。実際、真の王手駒の重みシェアは反則ありの手番で
**0.035〜0.037**（反則0の手番の 0.078〜0.084 の半分以下）・正規化エントロピーは
**0.99 = ほぼ一様**で、シェアの低さ自体が反則の予測子になっている。

### 型1（61手番）は「単に次点へ進んだだけ」ではない

受理された非玉手の開始時ソルバー p 順位は **4位以上が 46/57（v14）・41/43（v13）**。
一方で反則した手番の 47.2% / 54.0% は「ソルバー最善が最初から真に合法」だった。
**順位が下位の手に落ち着いている**ので、H2（候補固有の動的継続価値）の材料は残る。
ただしこれは**ソルバー p の順位であってエンジンの順位ではない**ので、
「浮かせたい候補があるのか」の判定は P0-4（`bin/check_probe` のエンジン順位と k\*）待ち。

### 残りの段への含意

- **γ（較正マップ）の枝は落ちた**。α（価格）と β（動的継続価値）が残る
- α / β の中止条件（P0-4）は**該当しない**が、枝ごとに強さが違う:
  **α は型1 で生きている**（k\* 中央値 2.8〜4.3・k ≤ 3 で 40〜44% が反転）一方、
  **β の支持は弱い**（対照つきの正味は 15〜19% で 20% 未満。型2 に限れば 22〜28%）
- 仮説の希釈（シェア 0.04・エントロピー 0.99）は H1/H2 とは別の第4の説明として
  実測された。これは価格でも継続価値でもなく**信念側**（CheckSolver の仮説重み）の
  問題なので、この issue の非目的（仮説列挙の拡張）と隣接する

### 撤回した数字（初版 run 33141692604）

初版は `view_from_model` に手数 0 を渡していて既知敵駒の鮮度窓が無制限だった。
**ソルバー由来の列だけ**が動いた（型の表・残り反則・記録の `p_legal` から作る
較正の値は不変）:

| | 初版 → 確定 |
| --- | --- |
| ソルバー最善が最初から合法（v14 / v13） | 52.8% → **47.2%** / 59.7% → **54.0%** |
| 破滅（v14 / v13） | 8 → **4手番** / 5 → **2手番** |
| CheckSolver 単体 p の平均予測（v14 / v13） | 51.2% → **60.1%** / 57.8% → **62.5%** |
| その Brier（v14 / v13） | 0.2168 → **0.2001** / 0.2153 → **0.2017** |

窓が正しくなるとソルバー単体の較正がはっきり良くなる（Brier が基底率予測
0.2270 / 0.2240 を初めて明確に下回る）ので、修正の向きも実測で裏づけられている。

## P0-4 の実測（run 33157124844・発見セット）

3 seed・思考予算 2000ms（記録を取ったアリーナと同じ）・型1（玉以外 → 玉の手）と
型2（玉以外 → 玉以外）の全決定点。判定は決定点ごとの seed 多数決。
**実再決定は初回決定と同じ prewarm 済み instance の継続**で、**反則の観測を
入れない対照（A）を同じ配管で取る**（PR #32 レビュー [P1] 対応後）。

| | vs v14 | vs v13 |
| --- | ---: | ---: |
| 監査対象の決定点（型1 / 型2） | 111（61 / 50） | 79（39 / 40） |
| 初回ランキングの首位が実戦の最初の反則と一致 | 72/111 (64.9%) | 53/79 (67.1%) |
| 現行の首位が既に玉の手（門の分母から外す） | 17/111 (15.3%) | 4/79 (5.1%) |
| **k\* ≤ 3 で玉の手が首位になる** | **22/94 (23.4%)** | **21/75 (28.0%)** |
| 　型1 だけ | 20/46 (**43.5%**)・k\* 中央値 **2.80** | 14/35 (**40.0%**) |
| 　型2 だけ | 2/48 (4.2%)・中央値 7.92 | 7/40 (17.5%)・中央値 5.34 |
| 反則注入後の実再決定が静的な次点と違う（生） | 68/111 (61.3%) | 55/79 (69.6%) |
| **対照（観測を入れない）が静的な次点と違う（ノイズ床）** | **49/111 (44.1%)** | **47/79 (59.5%)** |
| **実再決定が対照と違う = 反則の観測だけの効果（正味）** | **21/111 (18.9%)** | **12/79 (15.2%)** |
| 　型1 / 型2 の正味 | 7/61 (11.5%) / 14/50 (**28.0%**) | 3/39 (7.7%) / 9/40 (**22.5%**) |
| 最良の玉の手の p_legal の変化（注入の前後 / 対照） | +0.001 / −0.002 | +0.046 / +0.021 |

### 判定: 中止条件には該当しない（α は生きている。β は弱い）

中止条件は「k\* ≤ 3 で反転する決定点が 20% 未満 **かつ** 反則注入後に首位が変わる
決定点も 20% 未満」。**k\* 側がどちらの相手でも 20% を超える**ので、AND 条件の
中止条件には該当しない。ただし枝ごとに強さが違う:

- **α（価格）は生きている**。型1（issue の 61手番 / 118反則の塊）で k\* の中央値
  **2.80 / 4.32**、k ≤ 3 で **43.5% / 40.0%** が玉の手へ反転する。
  **型2（kakutori 型）は価格では動かない**（4.2% / 17.5%・中央値 5〜8）ので、
  **α を型で切らずに一律に掛けると型2 の捕獲プローブまで沈む**
- **β（動的継続価値）の支持は弱い**。生の「静的な次点と違う」率 61〜70% のうち
  **44〜60% はノイズ床**（反則の観測を入れなくても起きる）で、**正味は
  v14 18.9% / v13 15.2% = どちらも 20% 未満**。
  型別では **型2 の正味だけが 22.5〜28.0%** と 20% を超え、型1 は 7.7〜11.5%。
  つまり「反則を観測して候補順位が動く」のは kakutori 型のほうで、
  **玉逃げに落ち着く型1 では信念更新はほとんど効いていない**

### 再決定そのもののノイズ床が 44〜60% ある

対照（A）は B と**同じ prewarm 済み instance の clone・同じ視界（＝同じ残り反則＝
同じ価格）・同じ除外集合**で、反則の観測だけを入れずに選び直したもの。それでも
初回ランキングの次点と 44〜60% で食い違う。原因は `choose` のたびに
`Estimator::update` が壁時計デッドラインまで若返らせること（同じ入力でも粒子集合が
変わる）。**現行方策は同じ局面で選び直すだけで候補順位がこれだけ動く**。

これは issue #24 の教訓（「測定そのもののノイズが門と同じ大きさ」）の再現で、
**オフラインの再ランキング診断はすべてこの床を踏む**。H2 のように「順位が変わったか」を
数える指標は、**対照を同じ配管で取ってペア差で見ないと意味を持たない**
（初版は生の値を報告していて「66〜75% で次善手固定の仮定が外れる」と読んでいた。
run 33149098094 のその数字は撤回する）。

### 読み方の限界

- **初回ランキングの首位が実戦の最初の反則と一致するのは 64〜67%**。k\* の分布は
  「実戦のその決定そのもの」ではなく「**同じ状態から現行方策が引き直したときの
  分布**」と読むこと
- k\* は**ランキングがどれだけ動くか**の感度であって価格の測定ではない
  （価格の水準は P0-7 の影の価格。単位が勝率で、`foul_cost` の評価点とは
  曲線の形しか比べられない）
- 「反転しない unit」（kmax=30 まで玉の手が首位にならない）は v14 91 / v13 40。
  玉の手の p が低い決定点では**価格では原理的に反転しない**

## P0-5 の実測（run 33171943105・発見セット）

4 seed・思考予算 2000ms（記録を取ったアリーナと同じ）・王手中の全手番。
反則0の対照は反則あり手番と**同数**（`(局, 手数)` の辞書順で等間隔）。

**決定点の分母は P0-1 の「王手中の手番」から「受理手なし」を引いた数**になる
（v14 178 − 15 = 163、v13 139 − 23 = 116）。`truth_replay::for_each_decision_full`
は**受理された手**を単位に決定点を回すので、その手番で終局した（反則だけで
終わった）手番は決定点として現れない。P0-6 でも同じ理由で対象外にしてある。

| | vs v14 | vs v13 |
| --- | ---: | ---: |
| 決定点（反則あり / 反則0の対照） | 163 / 163 | 116 / 116 |
| unit（× 4 seed） | 652 / 652 | 464 / 464 |
| 実戦の反則/手番（記録） | 1.62 | 1.85 |
| **健全性: 反則0での仮想更新と初回ランキングの p_legal の最大差（平均 / p95）** | **0.0000 / 0.0000** | **0.0000 / 0.0000** |
| 復元: 最初に試した手が実戦と一致（static / real） | 76.8% / 76.8% | 77.6% / 77.6% |
| 復元: 受理手が実戦と一致（static / real） | 81.0% / 87.9% | 74.2% / 87.7% |

**健全性は完全に通っている**（仮想更新は `evaluate` の p_legal を厳密に再現する）。
復元も P0-4 の 64〜67% より高い（P0-4 は「初回ランキングの首位」だけを見るのに対し、
ここは手番全体を裁定するので受理手まで含めて比べられる）。

### 反則/手番（estimand `foul`）とペア差

比較の基準は **`current@shadow`**（同じ更新規則の現行方策）。

| arm | v14 反則/番 | Δ vs current | v13 反則/番 | Δ vs current |
| --- | ---: | ---: | ---: | ---: |
| `current@shadow` | 1.45 | — | 1.53 | — |
| `current@real`（実再決定＝正解基準） | 1.40 | −0.05 [−0.10, +0.00] | 1.55 | +0.02 [−0.03, +0.06] |
| `current@static`（次善手固定の仮定） | 1.79 | **+0.34 [+0.18, +0.50]** | 1.63 | +0.10 [−0.11, +0.28] |
| `alpha@k1.5` | 1.32 | −0.13 [−0.18, −0.08] | 1.39 | −0.14 [−0.21, −0.07] |
| `alpha@k2` | 1.19 | **−0.26 [−0.33, −0.18]** | 1.30 | **−0.23 [−0.33, −0.15]** |
| `alpha@k3` | 1.08 | **−0.37 [−0.51, −0.24]** | 1.12 | **−0.41 [−0.55, −0.29]** |
| `beta@k1l0.5` | 1.40 | −0.05 [−0.09, −0.01] | 1.48 | −0.05 [−0.08, −0.01] |
| `beta@k1l1` | 1.37 | −0.08 [−0.14, −0.03] | 1.48 | −0.05 [−0.09, −0.01] |
| `beta_order@l0.5` | 1.43 | −0.01 [−0.04, +0.00] | 1.53 | −0.00 [−0.01, +0.01] |
| `beta_order@l1` | 1.43 | −0.01 [−0.04, +0.01] | 1.52 | −0.01 [−0.02, +0.00] |
| `solver_greedy`（参考） | 1.15 | −0.30 [−0.57, −0.01] | 0.70 | −0.83 [−1.11, −0.59] |

**`current@static` が現行より 0.34 悪い**（v14）のは、`combine_score` の doc が
置いている「失敗枝の価値 = 次善手の価値 − 反則コスト」という仮定が**記述として
間違っている**ことの直接の測定。ただし実機は既に再決定しているので
（`current@real` 1.40 ≈ `current@shadow` 1.45）、**これは改善の余地ではなく
「モデルと実装のずれ」**。β はそのずれを価格へ織り込もうとして −0.05〜−0.08 しか
取れなかった。

### α の効き目は型1 に集中する（型2 の捕獲プローブは沈まない）

P0-4 の懸念「α を型で切らずに一律に掛けると型2 の捕獲プローブまで沈む」は、
**少なくとも v14 では起きない**:

| 型 | v14 current → k1.5 → k2 → k3 | v13 current → k1.5 → k2 → k3 |
| --- | ---: | ---: |
| 型1 `nonking_king`（issue の 61手番/118反則の塊） | 1.69 → 1.40 → 1.09 → **0.79（−53%）** | 1.78 → 1.53 → 1.31 → **0.98（−45%）** |
| 型2 `nonking_nonking`（kakutori 型） | 1.48 → 1.48 → 1.48 → **1.46（−1%）** | 1.27 → 1.19 → 1.12 → **1.00（−21%）** |
| 型3 `king_king` | 1.20 → 1.17 → 1.14 → 1.15 | 1.46 → 1.39 → 1.37 → 1.29 |
| 型4 `king_nonking` | 0.57 → 0.50 → 0.50 → 0.64 | 2.83 → 2.83 → 2.83 → 3.08 |

v14 では型2 が**まったく動かず**、削れているのはほぼ型1 だけ。v13 では型2 も
21% 落ちるので懸念は部分的に当たる。**k\* の分布（P0-4: 型1 の中央値 2.8〜4.3、
型2 は 5〜8）から予想されるとおりの挙動**で、価格は型を見なくても型1 を選んで削る。

### 破滅と真実指標は悪化しない

| | v14 current → k2 → k3 | v13 current → k2 → k3 |
| --- | ---: | ---: |
| 反則上限到達% | 1.2 → 0.9 → 0.9 | 1.7 → 1.3 → **0.6** |
| ≥8反則% | 0.0 → 0.0 → 0.0 | 0.4 → 0.0 → 0.0 |
| 受理% | 98.8 → 99.1 → 99.1 | 98.3 → 98.7 → 99.4 |
| 被一手詰め%（受理直後の真実） | 10.9 → 11.0 → 11.2 | 5.8 → 6.2 → 6.5 |
| 材料差（受理直後の真実） | 3.57 → 3.53 → 3.32 | 14.56 → 14.39 → 14.27 |

**破滅は改善する**（上限到達・≥8 とも減る）。被一手詰めのペア差は
v14 `k2` +0.0015 [+0.0000, +0.0051] / `k3` +0.0031 [−0.0049, +0.0137]、
v13 `k2` +0.0043 [+0.0000, +0.0142] / `k3` +0.0065 [+0.0000, +0.0174] で、
**α はわずかに被一手詰めを増やす方向**（v13 で 0.4〜0.7pt）。材料も少し落ちる。
つまり α は「反則を減らすかわりに材料と受けを少し払う」取引で、
**その取引が勝敗として得かは P0-6 でしか判定できない**。

### 反則0 estimand（新しいプローブを足す害）: α は無害

| arm | v14 反則/番（Δ） | v13 反則/番（Δ） |
| --- | ---: | ---: |
| `current@shadow` | 0.07 | 0.06 |
| `alpha@k2` | 0.05（−0.02 [−0.05, −0.00]） | 0.06（+0.00 [−0.01, +0.01]） |
| `alpha@k3` | 0.05（−0.02 [−0.06, +0.01]） | 0.08（+0.02 [−0.01, +0.05]） |
| `solver_greedy` | 0.36（**+0.29 [+0.13, +0.48]**） | 0.25（**+0.19 [+0.09, +0.33]**） |

**α の被一手詰めのペア差は全水準で厳密に +0.0000**（両相手）。
反則0の手番で新しいプローブを足していない。

対して **`solver_greedy` は反則0の手番を壊す**: 反則を +0.19〜+0.29 増やし、
被一手詰めも v14 で +0.046 [+0.009, +0.085]、材料差は +0.87 → **−1.57**。
P0-3 の「ソルバーに従えは解ではない」がそのまま再現された。

### 近似通過条件は**両相手で不通過** → β は中止

**初版は統計量を間違えていた**（PR #33 レビュー [P1]）。近似の良し悪しを
**符号付き平均**（`mean(shadow) − mean(real)`）で測っていたので、「ある unit で
+1 反則ずれ、別の unit で −1 反則ずれる」仮想更新が誤差 0 に見えてしまう。
**門には unit ごとの `|shadow − real|` を作ってから元対局 cluster で集約した値**を
使う（`paired_abs_ci`）。今回はどちらの統計量でも不通過だが、比率は一桁変わる。

| | v14 | v13 |
| --- | ---: | ---: |
| **受理までの反則数の絶対誤差 `\|shadow − real\|`（門）** | **0.150** | **0.097** |
| 符号付きの差（偏りの情報。門には使わない） | +0.046 [−0.002, +0.100] | −0.015 [−0.059, +0.029] |
| 受理手が一致した割合 | 95.6% | 94.6% |
| β の対現行改善量の最大 | `beta@k1l1` +0.080 | `beta@k1l0.5` +0.045 |
| **近似誤差 / 改善量** | **1.88**（符号付きなら 0.58） | **2.14**（同 0.33） |

（訂正後の値は再集計 run 33181993633。シャードは回さず、元の run 33171943105 の
`check-policy-*` artifact を `reaggregate_run_id` で集計し直したもの。健全性
「反則0での仮想更新と初回ランキングの p_legal の最大差 平均・p95 とも 0.0000」は不変）

事前登録の門は 0.25 以下。**どちらの相手でも大きく超える**ので、仮想更新で P1-β の
事前スコアは作れない。残るのは「候補ごとに推定器を clone して仮想 `MyFoul` を
注入して全候補を再評価する」設計だが、**そのコストが払えない**:

- 仮想更新（粒子を落として `CheckSolver` を作り直すだけ）でさえ **1回 p95 1304µs
  （v14）/ 1360µs（v13）**。P1-β は候補×候補なので、王手中の候補 30 本なら
  **約 40ms/決定**
- 正確版（候補ごとに `choose` をやり直す）は候補 30 本 × 思考予算 2000ms =
  **60秒/決定** で、P1 の門「思考 p95 の悪化 +200ms 以内」の約 300 倍

**β の効果量そのものも小さい**（−0.05〜−0.08 反則/手番、`beta_order` に至っては
−0.00〜−0.01 = 何も起きていない）ので、近似を諦めてコストを払う価値がない。

### 候補分布上の較正: H3 の門には届かないが、「符号すら逆」は**訂正**

| 手種 | v14 候補数 / 平均予測 / 合法率 / 方向つき較正誤差 | v13 |
| --- | ---: | ---: |
| `checker_capture` | 6760 / 0.176 / 0.134 / **+0.042 [+0.033, +0.053]** | 5512 / 0.181 / 0.142 / **+0.039 [+0.024, +0.052]** |
| `king` | 5796 / 0.564 / 0.478 / +0.086 [+0.061, +0.111] | 4112 / 0.596 / 0.518 / +0.079 [+0.042, +0.118] |
| `drop` | 39988 / 0.038 / 0.015 / +0.022 [+0.016, +0.028] | 31872 / 0.032 / 0.005 / +0.028 [+0.022, +0.034] |
| `other_board` | 10676 / 0.027 / 0.004 / +0.023 [+0.016, +0.032] | 8244 / 0.025 / 0.002 / +0.023 [+0.017, +0.030] |

**H3 の門（捕獲試みの較正誤差の CI 下限 > 0.1）は満たさない**（上限でも +0.053）
ので、**P1-γ は引き続き指示されない**。

ただし P0-2 の「捕獲試みの較正誤差は v14 +0.025 / v13 −0.009 で**符号すら逆**」は
**候補分布では成り立たない**: +0.042 / +0.039 と**両相手で同符号・CI が 0 を跨がない**。
P0-2 が見ていたのは「現行方策が選んだ手」だけなので、あの符号の食い違いは
**選択による見かけ**だった（P0-2 の doc が「γ で順位を変えた後の候補分布へは
外挿できない」と断ってあったとおり）。過信は小さい（+0.04）が**系統的に実在する**、
というのが候補分布での正しい読み。玉の手が最も過信（+0.08）なのは P0-2 と一致する。

## P0-7 の実測（run 33171943105・発見セット）——**撤回**

> **この節の数字は撤回する**（PR #33 レビュー [P1]、2026-08-28）。
> control と treatment を別 unit として共通キューへ積み、複数 worker で
> **同時実行**していた。思考予算は壁時計なので、同じ乱数 seed でも CPU 競合と
> 開始順が違えば粒子数と継続方策が変わり、**ペア差へスケジューリング差が混ざる**。
> しかも常に control を先に積んでいたので実行順も均衡していなかった。
> schema 2 では**1 worker が同じ `(決定点, seed)` の両 arm を背中合わせに走らせ、
> 偶数 seed 内で AB/BA を反転**する（checkpoint arena が実行順効果を cluster の
> 内側へ閉じたのと同じ契約）。`report` は schema 1 を弾き、実行順効果
> （先に走った arm − 後）を常時出す。**再計測待ち**。
>
> 以下は撤回した数字だが、**何を見るつもりだったか**の記録として残す
> （層別の切り方と「単位が違うので水準は比べない」の規約は変わらない）。

2 seed・思考予算 700ms・1局あたり2決定点。

| | vs v14 | vs v13 |
| --- | ---: | ---: |
| ペア / 元対局 | 372 / 98 | 328 / 88 |
| **影の価格（treatment − control、勝率単位）** | **−0.0161 [−0.0829, +0.0528]** | **−0.0610 [−0.1327, +0.0092]** |
| 残り1→0 で継続を始めなかった treatment | 6 本 | 8 本 |
| 継続の平均思考 | 557ms | 560ms |

全体はどちらも負（反則1個は勝率を下げる）だが **CI は 0 を跨ぐ**。
**単位が違うので水準は `foul_cost` と比べない**。読むのは形だけ。

### 形は「残り反則」ではなく「手数帯」と「相手の残り反則」に出る

| 手数帯 | v14 | v13 |
| --- | ---: | ---: |
| 序中盤 (0-49) | +0.0128 [−0.1037, +0.1329] | −0.0104 [−0.1275, +0.1222] |
| **中盤 (50-89)** | **−0.0933 [−0.1753, −0.0068]** | **−0.1308 [−0.2279, −0.0364]** |
| 終盤 (90-) | +0.0909 [−0.0500, +0.2353] | −0.0196 [−0.1389, +0.1122] |

| 相手の残り反則 | v14 | v13 |
| --- | ---: | ---: |
| 0-3（相手が上限間際） | +0.1364 [+0.0000, +0.3500] | **+0.2059 [+0.0333, +0.4000]** |
| 4-6 | −0.0152 [−0.1613, +0.1250] | **−0.1667 [−0.3194, −0.0139]** |
| 7-10 | −0.0282 [−0.1058, +0.0556] | −0.0702 [−0.1509, +0.0179] |

残り反則別は**どのバケットも CI が 0 を跨ぎ、単調でもない**（v13 の 残り1
−0.3750 [−0.5000, −0.1250]（n=8）だけが明確に負だが、これは treatment が
即 foul_limit 負けになる定義そのもの）。バケットあたり n=6〜96 と小さいので、
**残数依存が無いと結論するのではなく「この標本では検出できない」と読む**。

一方で**中盤は両相手とも CI が 0 を跨がず負**、**相手が上限間際なら価格は正**
（自分の1反則がタダになる）で、こちらは両相手で同方向。

**現行の `foul_cost = base × (10/残り)^1.333 × (相手残り/10)^diff_pow` との
形の違い**:

- 現行は**残り反則の単調増加関数**だが、影の価格の残数依存はこの標本では出ない
- 現行に**手数帯の項は無い**のに、実測で唯一はっきり出るのが中盤
- 現行の `foul_diff_pow`（相手の残数で割り引く項）は**方向として裏づけられた**
  （相手残り 0-3 で価格が正に転ずる）

### 終局理由（機構の確認）

| | v14 control / treatment | v13 control / treatment |
| --- | ---: | ---: |
| 詰み | 138 / 129 | 79 / 80 |
| 反則負け | 234 / 243 | 249 / 248 |

treatment 側で反則負けが増える（v14 234 → 243）のは反実仮想として正しい向き。

## P0 の判定（2026-08-28）

事前登録の判定条件に照らすと:

- **P1-α へ進む**（ただし P0-7 側の根拠は再計測待ち）。P0-7 が「影の価格の形が
  現行曲線と違う（残数依存が出ず、中盤と相手残数に出る）」を示し、**かつ** P0-5 で
  α が反則を減らして
  （−0.23〜−0.41、両相手で CI が 0 を跨がない・k に単調）破滅指標を悪化させない
  （上限到達・≥8 とも改善）。反則0 estimand でも無害（被一手詰めのペア差が
  厳密に 0）
- **P1-β は中止**。近似通過条件が**両相手で不通過**（0.58 / 0.33 > 0.25）で、
  正確版のコストは門の約 300 倍。効果量も −0.05〜−0.08 と小さい
- **P1-γ は引き続き指示されない**（候補分布の較正誤差 +0.04、門は > 0.1）。
  ただし P0-2 の「符号すら逆」は候補分布では成り立たないので訂正する

**P0-7 の再計測が済むまで、P1-α の根拠は P0-5 側だけ**である点に注意
（P0-5 のデータはこの欠陥の影響を受けない: `current@shadow` と `current@real` は
同じ `run_unit` の中で1つの worker が背中合わせに走らせている）。

### P1-α の形は「一律の倍率」ではない

issue の P1-α は「`check_cost = max(k × base_foul_cost, 床)`。P0-7 が曲線の形の
違いを示したときは倍率でなく残数別のテーブル」と書いてある。実測が示した違いは
**残数の次元ではなく手数帯と相手残数の次元**なので、素直な読み方は:

- 残数別テーブルへ置き換える根拠は無い（残数依存はこの標本では検出できない）
- 中盤で価格が高いという実測はあるが、**手数帯で価格を切る項は事前登録に無い**
  ので、P0-6 を通す前に足さない（発見セットを見てから水準を増やすことになる）
- したがって **P1-α は事前登録どおりの一律倍率で P0-6 へ進み**、手数帯の
  依存は P0-6 の後に別途検討する

### P0-6 の主 arm（事前に固定する）

**主 arm = `alpha@k2`**、secondary = `alpha@k3`。k2 を主にする理由:

- 事前登録した格子 {1.5, 2, 3} の中央で、両相手とも Δ の CI が 0 を跨がない
- k3 は反則削減が最大（−0.37 / −0.41）だが、材料差と被一手詰めの悪化も最大
  （v13 被一手詰め +0.0065）。P0-6 の門は**勝率**なので、払う側の小さい k2 を主に置く
- k1.5 は効果が小さい（−0.13 / −0.14）

**検証セット（未使用の `match_seed`）で回す**。発見セットを見てから水準を
変えないこと。

検証セットは **Arena run 33179939954**（`match_seed=20260828`・main vs
`estimator_v14` 104局。発見セットの run 32697854659 は `match_seed=20260815`）。
記録の健全性: **50.5%±9.7**（52勝51敗1分）・反則/局 6.43・時間切れ0・
思考 avg 1244ms / p99max 2787ms で、直前の記録帯に収まっている
（＝この記録は「現行方策の普通の対局」であって、偏った標本ではない）。
**この記録を P0-1〜P0-5 / P0-7 の測り直しに使ってはいけない**（選定バイアスの
分離が崩れる）。

## 動作確認（ローカル `records/`）

対人83局のうち終局まで到達した59局。**旧記録なので `chose.debug.p_legal` が無く
P0-2 は測れない**が、型の分類と破滅の内訳は同じ形で出る:
王手中の手番 209（反則あり 113。ソルバー最善が最初から合法 41/113 = 36.3%）、
真の王手駒の重みシェア 0.053 / 0.071、**破滅3手番はすべてマス・駒種とも一致**
= 発見セットと同じ「希釈」の署名。
型の分布は「玉の手で反則 → 玉の手で受理」75手番が最大で**アリーナとは違う**
（対人・旧方策の記録なので、判定に使うのは発見セットの方）。
