# 被王手の**前**の準備（王手耐性）P0（issue #34）

**位置づけ**: #28（詰み経済）と #31（王手中の反則経済）は、どちらも
「機構は動く・反則は減る・勝率に変換されない」で閉じた。#31 が測ったのは
**王手が来てから**の価格・順序・較正で、**王手が来る前の形**は測っていない。
ここで測るのは「王手が来たときの出口の本数と質が、王手前の自駒配置で
決まっているか」。

**runtime には何も入らない**（`bin/analyze` の集計セクションと
`bin/check_prep_probe` を足しただけ。粒子も回さない）。

| 段 | 何を識別するか | 実装 | 状態 |
| --- | --- | --- | --- |
| P0-1 連続王手の起点 | 指導4（玉がいた位置に王手の原因の駒がいる）の実測 | `bin/analyze` | 実装済み・計測済み |
| P0-2 較正プローブ | H1 / H2 と統計的 H0 | `bin/check_prep_probe` | 実装済み・計測済み |
| P0-3 w\* 監査 | 「浮かせたい候補が在るか」 | `bin/rank_probe` 相当 | **未実装**（P0-2 の結果待ち） |
| P0-4 一手強制の局所継続 | 破滅検出 | `bin/mate_continue` の配管 | **未実装**（P0-2 の結果待ち） |

段は**前が落ちたら次へ進まない**順序で並んでいる（issue #34 の中止条件）。

## 特徴量（`src/check_prep.rs`）

すべて `PlayerView` 相当（自駒＋持ち駒＋観測ログ）だけから決まるので値そのものに
ノイズは無いが、**意味は楽観上限**である: 飛び駒の利きは**自駒にしか遮られない**
（隠れた敵駒・ピン・両王手は扱わない）し、「空きマス」は「自駒がいないマス」。
名前もそのとおりに付けてある（`potential_covered` / `potential_flight`）。

- **F1 王手起点の潜在被覆**: 自玉 K に対する**王手起点マス集合 C(K)** は
  **玉位置ごとの固定集合**（盤端までの8方向の距離2以上の全マス＋桂の跳び起点2マス）。
  **自駒で止めない**のが要点で、自駒を置いて未被覆マスを集合から消せると
  「分母を縮めて被覆率を上げる」分母操作が成立してしまう。
  桂 / 直線 / 斜線を別列にし、**確定利き**（歩・桂・金銀の歩進）と
  **楽観レイ**（飛角香）も別列に持つ。**隣接8マスは F3 の領分で F1 には持たない**
  （説明力を見てどちらかを選ぶのではなく、設計時点で分担を固定する）
- **F2 攻撃面と潜在逃げ道**: `open_dirs`（盤端まで自駒に遮られていない方向）/
  `open_len`（自駒に当たるまでの連続マス数の合計）/ `flight_all`（隣接の自駒非占有）/
  `flight_shielded`（そのうち距離2が盤外か自駒 = 逃げ先の背後がすぐ塞がっている）。
  **P0 の診断専用**で P1 の arm には入れない
- **F3 隣接の状態数**: 8近傍を `自駒非占有・非被覆 / 自駒非占有・被覆 / 自駒占有` の
  3状態で数える（第1状態が現行 `king_holes`）。加えて**占有の有無によらない
  隣接の被覆数** `adj_covered` を持つ（下記の実測で王手の 8 割が隣接から来るため）
- **F4 出発マスの被覆**: 玉の手について「出発マスに**着手後も**玉以外の自駒の
  利きがあるか」

`K` は3つを区別する（issue #34 の用語）: `K_def_truth`（真実盤面の全合法手数）/
`K_def_gen`（診断側の候補生成のうち真に合法な本数）/ `K_def_runtime`
（観測ログ込みの runtime 候補生成での本数）。

## P0-1 連続王手の起点（`bin/analyze`）

「bot が玉の手で王手を解消 → 次の相手手番で再び王手」を取り出し、2回目の
真の王手駒を **(a) 前の玉位置 / (b) その隣接 / (c) それ以外** に分ける。
「前回と同じ駒が追った」は位置分類と混ぜず**別軸のフラグ**、両王手は別層で
「全王手駒を除去可能か」を手番単位で判定する。

**門は「(a)+(b) が過半」ではない**（頻度が高くても、被覆の有無で反則が変わらないなら
形を変える意味が無い）。`(a) の頻度 × F4 有無による反則差` と
`(b) の頻度 × 実王手駒マス被覆の有無による反則差`を**別々に**出し、
元対局 cluster CI を付けて合算も出す。

## P0-2 較正プローブ（`bin/check_prep_probe`）

**単位は「bot の全 handoff」**（= bot が受理手を指し終えた時点 = P1 が変えられる配置）。
被王手手番だけに条件づけると `F → 被王手 ← 相手の攻撃力 → K・反則` の
**collider** が開き、形に直接効果が無くても分離してしまう。
**主目的変数は「次の bot 手番に発生した王手中反則数」**（次に王手されなければ 0）。

計測時点は3つ: `pre`（直前の bot 受理手より前）/ **`handoff`（主 exposure）** /
`entry`（相手の王手着手後）。主解析は `handoff` の F と `pre→handoff` の差分で、
`entry − handoff` は「王手手が形を壊した量」として別列に持つ。

**終端の手番**（反則負けなどで受理手が無く `end.moves` に現れない手番）も
`check_prep::decision_snapshots` が1つ足す。落とすと**反則を積んで終局した手番だけが
系統的に欠測**して、門が甘くなる方向へ偏る。

段は3つ:

1. **構成概念検証**: 「実際の王手駒を玉以外で真に合法に取れたか」（oracle）を
   起点の被覆がどれだけ再現するか。**主指標は適合率**で、リフトではない —
   「被覆なし → oracle 陰性」はほぼ**論理的帰結**（被覆は真の利きの楽観上限なので、
   被覆が無ければ真の取り手も無い）で下駄を履いている。例外は handoff から entry の
   間に王手手が形を壊した場合だけ。
   **層ごとに独立の門を出す**（F1 = C(K)（距離2以上）/ 隣接 = F3 の領分 /
   両王手 = 別層）。層を分けずに1つの適合率にすると、実測で 8 割を占める隣接
   （別概念）が F1 の門を支配してしまう。**段3へ進む条件は F1 層の門**
   （主 estimand が F1 由来だから）で、**適合率の cluster CI 下限 > 0.5 かつ
   リフトの CI 下限 > 0**
2. **発火母数**（`anchor_probe` の教訓）: 観測された王手中反則のうち、直前の
   handoff の候補手の中に F を改善する到達可能な手があった割合。門は 20% 未満で中止
3. **主 estimand**: 層を固定したうえで主特徴量を Q90 / Q10 に置いたときの
   **周辺平均差**。モデルは **hurdle 分解**に固定（反則あり = ロジスティック /
   ありの場合の回数 = **ゼロ切断＋上側打ち切り** Poisson。`y == 残り反則` は
   「その手番で反則負けした」= `Y ≥ cap` としてしか分からないので上側確率で入れる）。
   合成する第2段は**観測される回数の期待値そのもの**
   `E[min(Y,cap)|Y>0] = Σ_{k=1}^{cap-1} k·P(Y=k|Y>0) + cap·P(Y≥cap|Y>0)`
   で、`min(E[Y|Y>0], cap)` の頭打ちでは**ない**（一般に一致しない。λ=1 / cap=2 で
   1.4180 vs 1.5820）。尤度側が上側打ち切りを扱っているのに g-computation でだけ
   別の量へ戻ると、判定値が静かにずれる。
   **係数は個別に読まない**（hurdle の2段の係数は符号が逆を向きうる）。
   secondary は Holm 補正で、**主特徴量は名前で保持する**（当てはめが落ちた特徴量を
   詰めると、繰り上がった secondary が「事前登録の主」として無補正で表示される）。
   当てはめが収束しなかった draw は捨てるので、**有効 draw が半数未満の bootstrap は
   CI を出さない**

層・共変量（総効果の回帰で調整する）: 手数・玉位置（盤端距離）・自駒数・持ち駒・
材料差・自玉周辺の自駒密度・残り反則・候補数・先後。**`K_def_*` と機構列は
入れない**（狙っている媒介変数と結果側の変数だから）。CI は元対局 cluster bootstrap。

### 元 Arena 実行の実験条件の検査

主回帰の前提は「同一 build / config の記録だけ」だが、**記録（JSONL）の `match` /
`end` にあるのは戦略名と相手名だけ**で、CSV meta の `source_fingerprint` は
解析バイナリの版でしかない。そこで `check-prep.yml` は同じ run の

- `arena-result-<相手>-s*/arena-summary.json`: candidate / baseline / 実効設定の指紋
  （`cand_config` / `baseline_behavior` / `cand_knobs`）/ 局数 / 時計 / 思考予算
- `arena-result-<相手>-s*/arena-games.jsonl`: commit と `match_seed`
- `arena-combined/combined.json`: 全シャード合算の局数

と、**API から取る元 run の `head_sha` / `conclusion`** も使い、
**シャード集合が 0..n-1 で完備・実効設定が全シャードで一致・局数と記録の本数が一致・
合算局数と一致・元 run が success 終了**を確かめてから解析する。
`expect_candidate` / `expect_match_seed` / `expect_commit` / `expect_games` /
`expect_shards` を渡せば期待値との照合も入る。**検証セットでは
`expect_match_seed` を必ず渡す**（発見セットと独立な seed であることを機械的に
固定するため）。どれか1つでも外れたら解析を進めない。

4点、実装で踏んだ落とし穴がある（PR #35 レビュー2〜4巡目）:

- **`match_seed` の照合は `match_seed_base` で行う**。記録に残る `match_seed` は
  arena.yml が付ける**シャードずらし**（`base + shard`）に、さらに `bin/arena` が
  基準ごとの定数を **XOR** した**実効値**なので、4シャードなら4値になるし base とも
  一致しない（実測: base 20260828 の4シャードで
  `11400714819309886408`〜`...411`）。`bin/arena` が `match_seed_base` /
  `match_seed_shard` / `match_seed_env` / `match_seed`（実効値）を
  `arena-summary.json` へ明示的に残すようにして、**式を複製せずに**照合する
  （実効 seed の式は `baseline_match_seed` の1箇所で、`cargo test` が定数ごと固定する）。
  記録された `match_seed_shard` の集合が `0..n-1` で完備かも、artifact 名とは
  独立の裏取りとして検査する
- **`arena-combined` は必須**。シャード番号の連続性だけでは**末尾**の欠落
  （`{0,1,2}` は `0..n-1` に見える）を拾えず、末尾シャードの `arena-result-*` と
  `arena-records-*` が対で落ちると「局数 == 記録本数」も通ってしまう
  （実測: 78 == 78 で通過）。合算局数との照合が唯一の fail-closed な経路なので、
  取れなければ止める
- **base seed の「ラベル」と実際の対局条件の一致は `bin/arena` の起動時に検査する**。
  `ARENA_MATCH_SEED_BASE` / `ARENA_SHARD` は下流へ渡すラベルでしかなく、対局条件を
  決めるのは `ARENA_MATCH_SEED` だけ。関門はラベル側（`match_seed_base`）しか見ないので、
  食い違うと `expect_match_seed` を指定しても通ってしまう（実測:
  `ARENA_MATCH_SEED=7 ARENA_MATCH_SEED_BASE=20260829 ARENA_SHARD=0` で
  summary は `match_seed_base=20260829` を記録するのに対局は 7 由来）。
  `check_seed_provenance` が「3値のどれか1つでも指定されたら3値が揃い
  `match_seed == base + shard`」を要求し、違えば run 自体を落とす
  （**式を check-prep へ複製しない**ための場所でもある。数値 env が指定されているのに
  parse できないときも黙って `None` にせず落とす = 「指定していない」と区別できないと
  この検査をすり抜けるため）。workflow 側の
  `match_seed_env == base + shard` は、**この検査より前の binary で回した run**
  （base は残るが未検査）を弾くための裏取り
- **その裏取りを `jq` の数値演算で書いてはいけない**。jq は IEEE-754 倍精度なので
  2^53 を超える整数を正確に加算できず、seed の型（`u64`）が普通に踏む範囲で
  **正常な run を拒否する**（実測: 実際の `arena-summary.json` に
  base=9007199254740993 / shard=1 / env=9007199254740994 が正確に残っているのに、
  `jq 'select(.match_seed_env != (.match_seed_base + .match_seed_shard))'` は
  MISMATCH を出す。標準的な 64-bit 乱数 seed で検証セットへ進めなくなる）。
  この段は `scripts/ci/verify_seed_provenance.py` に切り出し、Python の `json` で
  **整数のまま**読んで任意精度で比べる。関門がここにしか無いので
  `--self-test`（2^53 超・u64 上限近く・ラベル食い違い・シャード欠落・base 無し）を
  CI が毎回先に走らせる。**`jq ... | tostring` は jq 1.7 の number preservation で
  桁落ちしない**が、演算を1回でも通すと壊れるので、この段では読み出しごと Python へ寄せた

## 実測（発見セット: Arena run 32697854659 = main vs v13 / v14 各104局、`match_seed=20260815`）

計測は Check prep run 33247289035（`arena_run_id=32697854659`。**追加の対局コストはゼロ**）。
旧 `records/`（対人59局・旧 build）は構成概念検証と頻度確認にだけ使い、統合係数は出さない。

### P0-1 連続王手の起点

| | vs v13 | vs v14 | 対人59局（旧 build・参考） |
| --- | ---: | ---: | ---: |
| 玉の手で王手を解消した手番 | 246 | 304 | 170 |
| うち次の相手手番で再び王手 | 122 (49.6%) | 137 (45.1%) | 69 (40.6%) |
| **(a) 前の玉位置** | 50 (41.0%) / 0.50 反則/件 | 43 (31.4%) / 0.79 | 5 (7.2%) / 0.80 |
| **(b) その隣接** | 20 (16.4%) / 0.45 | 35 (25.5%) / 0.63 | 36 (52.2%) / 0.69 |
| (c) それ以外 | 52 (42.6%) / 0.50 | 59 (43.1%) / 0.71 | 28 (40.6%) / 1.93 |
| (a) で「同じ駒が追った」 | 38/50 (76%) | 26/43 (60%) | 2/5 |
| 寄与量 (a) × F4 | +0.100 [−0.131, +0.299] | +0.183 [−0.001, +0.387] | +0.036 [−0.068, +0.212] |
| 寄与量 (b) × 王手駒マス被覆 | +0.002 [−0.179, +0.121] | +0.006 [−0.137, +0.162] | −0.135 [−0.821, +0.316] |
| 合算 (a)+(b) | +0.101 [−0.193, +0.359] | +0.189 [−0.058, +0.443] | −0.099 [−0.749, +0.394] |

**指導4の観察そのものは当たっている**: 玉の手で王手を解消すると **45〜50% で次の手番も王手**
になり、その王手駒は (a)+(b) で **57%**（v13 57.4% / v14 56.9%）が前の玉位置かその隣接。
(a) の 60〜76% は**同じ駒が追ってきた**もので、「玉がいた位置に王手の原因の駒がいる」は実在する。

**それでも門は通らない**: 寄与量（`頻度 × 被覆の有無による反則差`）は両相手とも
**cluster CI が 0 を跨ぐ**（v14 の (a) が下限 −0.001 で境界）。方向は正（被覆があると反則が減る）で
両相手とも一致するが、**頻度が高いことと、形を変えると反則が減ることは別**というのが
この段の結論。対人（旧 build）は (a) が 7.2% しかなく (b) が 52.2% で分布そのものが違うが、
**bot の build も相手も違う**ので人間差には帰属できない。

### P0-2 較正プローブ

| | vs v13 | vs v14 | 対人59局（旧 build・参考） |
| --- | ---: | ---: | ---: |
| handoff / 元対局 | 5310 / 104 | 5070 / 104 | 2075 / 59 |
| ① 次の相手手が王手 | 530 (10.0%) | 587 (11.6%) | 253 (12.2%) |
| ② `K_def_truth` 平均 | 3.52 | 3.26 | 2.30 |
| 　真に合法な解消手 ≤1本 | 63/530 (11.9%) | 141/587 (24.0%) | 101/253 (39.9%) |
| ③ 反則 0 / 1 / 2+ | 391 / 80 / 59 | 409 / 113 / 65 | 135 / 40 / 78 |
| 　残り反則で打ち切り | 23 | 15 | 20 |
| 候補生成が取りこぼした手番 | 5/530 | 2/587 | 1/253 |
| `K_def_runtime < K_def_gen` | 0 | 0 | 0 |

**issue 本文の「出口≤1本が 37%」は反則した手番に条件づけた値だった**。反則0の手番も含めた
分布では **11.9%（v13）/ 24.0%（v14）** で、条件づけの効果が大きい。
**候補生成は律速ではない**（真の解消手の取りこぼしは 0.4〜0.9%、runtime 生成と診断側は完全一致）。

#### 実王手起点の分布（**この節が主特徴量の形を決める**）

| | vs v13 | vs v14 | 対人（参考） |
| --- | ---: | ---: | ---: |
| **隣接（距離1 = F3 の領分）** | 444/530 (**83.8%**) | 485/587 (**82.6%**) | 207/253 (81.8%) |
| C(K)（距離2以上 = F1 の領分） | 86 (16.2%) | 102 (17.4%) | 46 (18.2%) |
| C(K) のクラス（直線/桂/斜線） | 31 / 29 / 26 | 51 / 34 / 17 | 19 / 15 / 12 |
| C(K) の距離 2 / 3 / 4 / 5+ | 65 / 13 / 4 / 4 | 66 / 21 / 8 / 7 | 29 / 12 / 4 / 1 |
| 打ち王手 | 173 (32.6%) | 186 (31.7%) | 89 (35.2%) |

**実際の王手の 8 割は隣接（距離1）から来る**。F1 は設計どおり隣接を持たない（F3 の領分）ので、
**F1 単独では実王手起点の 1/6 しか分母に持たない**。距離2以上の中では距離2が 3/4 を占める。

#### 段1: 構成概念検証 — **層ごとに独立の門。F1 層で両相手とも通過**

| | vs v13 | vs v14 |
| --- | ---: | ---: |
| oracle 陽性（玉以外で王手駒を真に合法に取れた） | 286/530 (54.0%) | 255/587 (43.4%) |
| **F1 層（C(K) = 距離2以上）** | 86件: 被覆あり 39 → 陽性 35 / なし 47 → 陽性 0 | 102件: 被覆あり 33 → 陽性 29 / なし 69 → 陽性 0 |
| 　適合率 | **0.897 [0.795, 0.980]** | **0.879 [0.733, 1.000]** |
| 　うち確定利きのみ | 0.963 [0.870, 1.000] | 0.905 [0.700, 1.000] |
| 隣接層（F3 の領分） | 442件: 被覆あり 259 → 陽性 251 / なし 183 → 陽性 0 | 483件: 被覆あり 236 → 陽性 226 / なし 247 → 陽性 0 |
| 　適合率 | 0.969 [0.946, 0.988] | 0.958 [0.928, 0.981] |
| 両王手層 | 2件（被覆あり1 → 陽性0） | 2件（被覆ありが 0 件で判定不能） |

**門（F1 層の適合率 CI 下限 > 0.5 かつ リフト CI 下限 > 0）は両相手で通過**。
層を分けずに1つの適合率にすると 0.957 / 0.948 になるが、**それは 8 割を占める
隣接層（別概念）の値にほぼ等しい**ので F1 の門にはならない。
確定利きだけに絞っても適合率は下がらない（むしろ v13 は上がる）ので、
「飛び駒の利きは自駒にしか遮られない」という楽観近似はこの用途では十分。

#### 段2: 発火母数 — **両相手で通過**

| | vs v13 | vs v14 |
| --- | ---: | ---: |
| 王手中反則のうち、主特徴量を改善する候補があった | 95.0% [90.9, 98.6] | 97.2% [94.8, 99.4] |
| 同、実際の王手起点を被覆する候補があった（後知恵の上限） | 98.6% [96.4, 100.0] | 98.9% [97.2, 100.0] |
| 全 handoff のうち主特徴量を動かせた決定点 | 97.9% | 98.1% |

門（20% 未満で中止）を大きく上回る。**「浮かせたい候補が無い」型の失敗ではない**。

#### 段3: 主 estimand — **両相手とも 0 を跨ぐ（不通過）**

Q90 − Q10 の周辺平均差（反則/手番。**負 = その特徴量が高いほど次手番の王手中反則が
少ない**）。cluster bootstrap 1000 反復:

| 特徴量 | vs v13 | vs v14 |
| --- | ---: | ---: |
| **f1_cov_w（事前登録の主）** | −0.0132 [−0.0531, +0.0228] p=.48 | −0.0288 [−0.0626, **+0.0009**] p=.060 |
| f1_cov_sure/total | −0.0139 [−0.0511, +0.0196] | −0.0136 [−0.0391, +0.0176] |
| adj_cov_frac（隣接の被覆） | **+0.0607 [+0.0184, +0.1304]** Holm p<.001 | +0.0191 [−0.0110, +0.0553] |
| f13_cov（F1 と隣接の混合） | +0.0334 [−0.0037, +0.0829] Holm p=.35 | +0.0028 [−0.0258, +0.0406] |
| f3_open_uncovered（現行 `king_holes`） | **−0.0318 [−0.0648, −0.0098]** Holm p=.012 | −0.0130 [−0.0429, +0.0102] |
| f3_own_occupied | −0.0056 [−0.0587, +0.0509] | −0.0151 [−0.0584, +0.0410] |
| f2_open_len | +0.0055 [−0.0381, +0.0449] | +0.0202 [−0.0177, +0.0562] |
| f2_flight_all | +0.0145 [−0.0187, +0.0427] | +0.0231 [−0.0118, +0.0582] |

参考: **F1 と隣接を同時に入れた調整済み効果**（secondary・事前登録外。上の主 estimand は
どれも**総効果**で、他の F は共変量に入れない = 事前登録どおりなので、F1 と隣接が相関して
いれば互いに交絡する。見てから作った量なので判定には使わない）:

| | vs v13 | vs v14 |
| --- | ---: | ---: |
| f1_cov_w｜隣接被覆を固定 | −0.0155 [−0.0560, +0.0219] | −0.0293 [−0.0654, **+0.0006**] |
| adj_cov_frac｜F1 を固定 | **+0.0612 [+0.0186, +0.1321]** | +0.0210 [−0.0102, +0.0596] |

**事前登録の主 estimand は両相手とも CI が 0 を跨ぐ**（v14 は上限 +0.0009 の境界、
v13 は明確に null）。判定は「v13 / v14 の両相手で同方向かつ有意」なので、
**統計的 H0 を棄却できない**。調整済みでも、有意になるのは v13 の隣接だけで、
**v14 の F1 は上限 +0.0006 で 0 を跨ぐ**。

一方で**符号は4つの量すべてで両相手一致している**、というのがこの段の実質的な所見:

- **F1（距離2以上の起点の被覆）は負** = 遠隔の王手起点に利きを作ると反則が減る（指導1 の向き）
- **隣接の被覆は正** = 隣接を被覆すると反則が**増える**（指導3 と逆の向き）
- **`f3_open_uncovered`（穴の数）は負** = 穴が多いほうが反則が少ない（= 逃げ道が要る）

**この2つは逆を向いているので、混ぜた `f13_cov` は打ち消し合ってゼロになる**
（v14 +0.0028）。「王手起点の被覆」という**単一の構成概念は成立していない**: F1 と隣接は
別物として扱わなければならない（issue #34 の P1 が arm を F1 単独 / F3 単独 / F1+F3 と
分けていたのは、この意味で正しい設計だった）。指導2（逃げる）と指導3（固める）の衝突も、
`f3_open_uncovered` が負であることに現れている。

なお `--origin-weights` / `--adj-share` は P0-2 の出力がそのまま貼れる形で出す
（発見セットの実測: v14 `knight:34,line:51,diag:17;d2:66,d3:21,d4:8,d5+:7` / `--adj-share 0.83`、
v13 `knight:29,line:31,diag:26;d2:65,d3:13,d4:4,d5+:4` / `--adj-share 0.84`）。

#### 修正前の数値との差（PR #35 レビュー [P1] の効き）

打ち切りつき周辺平均を `min(E[Y|Y>0], cap)` から `E[min(Y,cap)|Y>0]` へ直した効果は、
主 estimand では小さい（v14 −0.0298 → −0.0288 / v13 −0.0140 → −0.0132）が、
**境界の判定は動いた**: 調整済みの `f1_cov_w｜隣接固定`（v14）は
**−0.0302 [−0.0686, −0.0030]（有意）→ −0.0293 [−0.0654, +0.0006]（0 を跨ぐ）**。
段1 の適合率も、層を分けたことで 0.948/0.957（混合）→ **0.879/0.897（F1 層）** へ下がった。
**修正前に docs へ固定した数値は使わないこと**（この節の値が正）。

## 落とし穴（同種の判定をやり直すときに先に読む）## 落とし穴（同種の判定をやり直すときに先に読む）

- **被王手手番に条件づけない**。`F → 被王手 ← 相手の攻撃力 → K・反則` の collider が開く。
  実際、issue 本文の「出口≤1本が 37%」は反則した手番に条件づけた値で、条件づけない分布では
  11.9〜24.0% だった
- **構成概念検証をリフトで読まない**。被覆は真の利きの楽観上限なので「被覆なし → oracle 陰性」は
  ほぼ論理的帰結で、リフトは下駄を履く（実測 +0.95）。中身は適合率のほう
- **終端の手番を落とさない**。反則を積んで終局した手番は `end.moves` に現れないので、
  素直に決定点を回すと**反則が多い側が系統的に欠測**する
- **F1 と隣接を1つの量に混ぜない**。符号が逆なので混合は効果を消す
- **主 estimand を係数で読まない**。hurdle の2段の係数は符号が逆を向きうるので、
  合成した周辺平均で読む
- **打ち切りを尤度だけで扱って g-computation で忘れない**。観測するのは
  `min(Y, cap)` なので周辺平均も `E[min(Y,cap)|Y>0]` で出す。`min(E[Y|Y>0], cap)` の
  頭打ちは一般に一致せず（λ=1 / cap=2 で 1.4180 vs 1.5820）、**境界の判定が動く**
  （実測で v14 の調整済み F1 が有意 → 0 を跨ぐへ変わった）
- **段1 の門を層で分ける**。F1（距離2以上）と隣接（F3）を1つの適合率にすると、
  実測で 8 割を占める隣接が F1 の門を支配する（0.948 vs F1 層 0.879）
- **収束しなかった当てはめを成功にしない**。有限な値が返るだけでは足りず、
  bootstrap の draw に混ざって CI を静かに歪める。**有効 draw が半数未満なら
  CI を出さない**
- **主特徴量は名前で保持する**。落ちた特徴量を詰めると、繰り上がった secondary が
  「事前登録の主」として無補正で表示され Holm も免れる
- **記録だけでは実験条件を検査できない**。`match` / `end` にあるのは戦略名と相手名だけで、
  CSV meta の `source_fingerprint` は解析バイナリの版。元 Arena 実行の
  `arena-summary.json` / `arena-games.jsonl` / `combined.json` と、API から取る
  `head_sha` / `conclusion` まで照合する。
  **記録の `match_seed` は実効値**（シャードずらし ＋ 基準ごとの XOR）なので、
  base seed と直接比べてはいけない（`match_seed_base` を明示的に残して照合する）。
  **完備検査は末尾の欠落に効くものを選ぶ**: シャード番号の連続性も
  「局数 == 記録本数」も末尾欠落を通してしまうので、合算局数との照合を必須にする。
  **発見セットの run 32697854659 は commit 886ef75 が `ARENA_GAMES_JSON` より前で
  `arena-games.jsonl` を持たない**ので、`match_seed` を記録から確かめられない
  （CLAUDE.md の `match_seed=20260815` の裏は artifact からは取れない）。
  発見セットには seed の独立性は要らないが、**この run は検証セットには使えない**
