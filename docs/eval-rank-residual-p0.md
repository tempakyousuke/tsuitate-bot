# 人手採点を候補手評価へ戻す — P0（棋譜外へ一般化するかの判定）

2026-08-26。issue #24 の P0。**この文書は P0 の記録**で、P1（bounded residual の
runtime 統合）へ進むかどうかの判断を「判定」節に書く。

## 位置づけ（変えないこと）

- P0 の目的は二択を決めること: ①棋譜外へ一般化する信号がある → 小さな残差モデルを
  P1 で統合する ②同一棋譜の暗記しかできない → **実装せず**、必要な追加レビュー数と
  局面層を確定する
- scenario suite を arena の代わりにしない。**最終採否は通常 arena のガントレット**
- `value_nn`（勝敗教師）を置き換えない。人手の局所順位教師とは役割が違う
- 未採点候補を悪手として扱わない（`?` は欠測）
- 真実盤面・実戦の正解手・コメント文・棋譜名を実対局の特徴量に入れない

## 実装

| 部品 | 場所 | 役割 |
| --- | --- | --- |
| eval → 決定点の復元 | `src/eval_rank.rs` | `## N手目` → `ply = N-1`、`### N手目（和名(USI)の反則後）` → 同じ ply ＋ 元棋譜の `*illegal` をその USI まで注入。元 KIF は `scenarios/archive/<eval名>.kif`（無ければ `scenarios/<eval名>.kif`） |
| 特徴量 | `eval_rank::feature_row` | 入力は `PlayerView` と `CandidateScore` **だけ**。46列（内訳・候補集合内の位置・状態・着手） |
| エクスポータ | `bin/export_eval_rank_data` | 決定状態 × seed で `choose` → 候補ランキングと採点を結合して CSV ＋ summary JSON |
| 分析 | `scripts/eval_rank/fit_residual.py` | leave-one-source-KIF-out ＋ 訓練 fold 内の nested CV（α）。pairwise ridge |

### 復元の関門（黙って落とさない）

- 復元した `(ply, fouls)` に対応するシナリオ kif があれば `scores=` の**完全一致**を
  検査し、ずれたら停止する。反則後ブロックの対応が `foul_blocks.py` の FOUL_MAP と
  一致することも `cargo test 反則後ブロックの対応がpython側の表と一致する` が検査する
  （別の手目・別の反則後サブ状態へ接続する事故は PR #3 で実際に起きた）
- **採点済みなのに現行候補集合に無い手**は `in_candidates=0` の行として残す。
  黙って落とすと候補生成 recall が測れず、「未採点候補の除外による見かけの改善」と
  同じ穴が分析側に開く
- 現行候補にあるが eval に未収載の手、ランキングが取れなかった決定状態も summary に件数で出る

### 特徴量の健全性

`feature_row` の入力は自分側の完全既知情報（`PlayerView`）と bot 自身が計算した
内訳（`CandidateScore`）だけで、`Position`（真実）は型として渡らない。
`cargo test 特徴量は相手の駒配置に依存しない` が「相手の駒を全部消した別の真実」から
作った view で行が変わらないことを実測で確かめる。
タイブレーク乱数は `CandidateScore::tiebreak` へ分離したので、特徴量に載る `adjust` は
乱数を除いた決定的な補正だけ。USI 文字列・絶対マスの one-hot・棋譜名・ply の固有 ID は
そもそも列に無い。

### 分割（ここを間違えると全部無意味になる）

採点 5,035 件のうち **94.4% が `quest_20260731` と `quest_0809` の2局由来**なので、
行・候補・決定点のランダム分割はほぼ確実にリークする。分割単位は元 KIF だけにし、
同じ棋譜の通常ブロック・反則後ブロック・全 seed を必ず同じ fold へ置く。
主評価は leave-one-source-KIF-out、α は訓練 fold 内の nested CV（これも元 KIF 単位）。
指標は source KIF ごとの macro 平均で、行の micro 平均は主張に使わない。
seed は独立標本として数えない（決定状態ごとに平均してから状態を平均する）。

## 実測（2026-08-26）

```
cargo run --release --bin export_eval_rank_data -- --out data/eval_rank.csv --seeds 4 --jobs 4
python3 scripts/eval_rank/fit_residual.py data/eval_rank.csv --out /tmp/p0.md
```

思考予算 2000ms（既定）・seed 0..3。決定状態 272（うち 180 は対応シナリオがあり
`scores=` の完全一致を確認）、採点 5,035 件・未採点 27 件、候補行 123,816。
`data/` は gitignore（数十MB・壁時計予算で粒子数が揺れるので再現は「同じ結論が出る」
までで、行の一致ではない）。

### 指標の作り方（PR #25 レビュー反映）

- **未採点の top-1 を仮の点で埋めない**。`?` は欠測という契約どおり、得点系は
  「top-1 が採点済みの決定状態」だけで条件つきに数え、「未採点手を選んだ率」を
  別指標として並べる。仮 4 点で混ぜると、未採点選択が増える側の腕の得点が、
  選ばれた手の実点次第で反転しうる（初版の −0.426 はこの形で出ていた）
- 条件つきの平均は腕ごとに母集団が変わる（未採点へ逃げた分だけ悪手が母集団から
  消える）ので、**合否は「両腕とも top-1 が採点済み」の決定状態だけの対比較**で見る
- **タイブレーク乱数はどの特徴量にも載せない**。`adjust` だけでなく `score` /
  `static_score` も乱数を含むので3つとも引き、`rank` / `score_gap_top` も
  乱数を除いたスコアで付け直す。乱数込みの実際の着手順位は識別子列 `engine_rank`
  に置き、現行方策の baseline はそちらで再現する（同点の破り方まで一致させる）

### 採点の被覆（**これが効き方を決めている**）

- 1決定状態あたりの候補 **113.8 手、うち採点済み 18.3 手（16.1%）**
- 現行 score の **top-1 が採点済み 93.4% / top-3 87.2% / top-10 75.7%**
- 採点済みなのに現行候補集合に無い手は 204 行（**候補生成 recall 99.7%**）

`make_eval` は「その時点の上位15候補＋実戦手」しか列挙しないので、順位学習の教師も
評価指標も採点済みの部分集合しか見られない。一方**argmax はいつでも未採点側へ
逃げられる**。

### 素の残差ランカー（現行 score を捨てた形）

両腕とも top-1 が採点済みの決定状態だけの対比較:

| holdout | 状態数（対 / 全体） | top-1 得点 現行→模型 | 0〜2点 top-1 率 現行→模型 |
|---|---:|---|---|
| arena-check-flee02 | 1 / 1 | 10.00 → 10.00 | 0.000 → 0.000 |
| arena-check-flee03 | 1 / 1 | 10.00 → 10.00 | 0.000 → 0.000 |
| arena-check-flee04 | 1 / 1 | 7.50 → 6.50 | 0.000 → 0.000 |
| arena-check-foul02 | 5 / 7 | 3.40 → 3.00 | 0.500 → 0.500 |
| arena-check-foul03 | 7 / 7 | 3.07 → 2.83 | 0.750 → 0.655 |
| arena-recap01 | 1 / 1 | 6.00 → 6.00 | 0.500 → 0.500 |
| **quest_0809** | 75 / 106 | 5.21 → **4.22** | 0.078 → 0.169 |
| **quest_20260731** | 107 / 148 | 5.38 → **4.70** | 0.305 → 0.400 |
| **macro** | — | **6.320 → 5.906** | 0.267 → 0.278 |

条件つき macro（母集団が動くので参考）: top-1 得点 6.417 → 5.661・
8〜10点 top-3 recall 0.682 → 0.389・regret 2.686 → 3.398・
**未採点手を選んだ率 0.081 → 0.148**・pairwise concordance 0.763 → 0.780。
cluster bootstrap（元 KIF 単位・8クラスタなので参考値）は
top-1 得点 −0.741 [−1.211, −0.321] / concordance +0.013 [−0.098, +0.172]。

**順位一致（concordance）は上がるのに、選ぶ手は悪くなる。** 学習しているのは
「採点済みの手どうしの並べ方」であって「実際に指す手」ではない。

### bounded residual（P1 の統合形）の W スイープ

素の残差ランカーは現行 score を捨てた形なので不利だ、という反論を潰すために、
issue の P1 が提案する `pred = score + W × cap × tanh((r − r̄)/cap)`（cap=2点）を
そのまま holdout で掃いた。**W=0 は現行と完全一致**。得点は W=0 との対比較。

| W | macro top-1 得点 W=0→W | macro 0〜2点 top-1 率 | quest_0809 の差 | quest_20260731 の差 | 未採点率 | concordance |
|---:|---|---|---:|---:|---:|---:|
| 0 | 6.417 → 6.417 | 0.264 → 0.264 | ±0 | ±0 | 0.081 | 0.763 |
| 0.5 | 6.417 → 6.416 | 0.264 → 0.264 | +0.010 | −0.019 | 0.081 | 0.763 |
| 1 | 6.417 → 6.484 | 0.264 → 0.254 | −0.019 | +0.015 | 0.084 | 0.765 |
| 2 | 6.418 → 6.571 | 0.264 → 0.246 | +0.097 | −0.012 | 0.085 | 0.767 |
| 4 | 6.436 → 6.650 | 0.263 → 0.218 | +0.080 | +0.037 | 0.064 | 0.767 |

**macro は正に見えるが、中身は 14 決定状態**。W=4 の macro +0.21 はほぼ
`arena-check-foul02`（4.33 → 6.32、7状態）と `arena-check-foul03`（3.07 → 3.18、
7状態）で、**十分な状態数がある quest 2局は ±0.1 の横ばい**（+0.080 / +0.037）。
1決定状態のクラスタも106状態のクラスタも同じ重みで平均する macro の性質がそのまま
出ているだけで、P0 の門（大きい quest 2局で +0.5）には遠い。

> 初版はここを「どの W でも現行を超えない」と書いていたが、それは未採点の top-1 を
> 一律4点で埋めていた副作用だった（PR #25 レビュー指摘 P1）。指標を直すと
> **符号は変わる**。変わらないのは「大きい2局では動かない」ほう。

### 層別・seed・学習曲線

- 層別（holdout 予測のみ・条件つき）: 被王手 6.13 → 5.01・反則後 5.04 → 3.47 と
  **小さい eval 群で大きく逆行**する。改善らしきものは序中盤の concordance +0.014 程度
- seed を変えたときの選択一致率は 0.635 → 0.644（粒子揺れへの頑健性は変わらない）
- 学習曲線（訓練に使う元 KIF を 1..7 本に振る）: concordance 差は
  −0.139 / −0.056 / −0.038 / −0.003 / +0.008 / +0.014 / +0.017 と本数に応じて
  上がるが、**標準偏差はどこでも ±0.21〜0.29**。分散が「どの棋譜を holdout に
  したか」で決まっている（棋譜数が本数として律速）

### 学習された係数（全データ・α=1、標準化後）

内側 CV が選ぶ α は 1（最小）。係数は `static_score +19.7` / `score −11.0` /
`score_gap_top +9.5` / `gain +5.0` / `static_gain −3.4` と、ほぼ共線な
スコア系の差分方向に大きな重みが乗る**脆い解**で、その次に
`to_rank_own +1.12` / `dist_own_king −1.09` / `checker_removal −0.88` /
`risk +0.67` / `is_drop −0.59` / `mover_value −0.42` が続く。
`score` 係数 −11.0 に対しそれ以外のノルム 22.8 なので、**現行 score の再現では
ない**（暗記でもない: USI・絶対マスは列に無い）。

小さい方の係数の向きは `bin/eval_features` の局面内中心化回帰（玉候補距離1 +1.08 /
安さ +0.88 / 打ち −0.43）と一致する。ただし上のとおり、この一致は**選ぶ手の改善には
変換されていない**。

## 判定

**P0 不合格。runtime 実装（P1）をしない。**

| 合格条件 | 実測 | 判定 |
|---|---|---|
| 大きい quest 2局の完全 holdout で top-1 得点 +0.5 以上 **または** 0〜2点 top-1 率 25% 以上の相対減 | 0809: −0.99 / −117%、31: −0.69 / −31%（いずれも対比較） | 否 |
| macro pairwise concordance +5pt 以上 | +0.017 | 否 |
| 被王手・反則後の小さい eval 群で top-1 −0.5 超の逆行がない | 最悪 −1.00（arena-check-flee04） | 否 |
| 改善が未採点候補の除外・特定USI/マスの暗記・1棋譜で説明されない | **未採点選択 0.081 → 0.148**、fold 間で concordance の符号が反転（+0.026 / +0.010 / −0.196 / −0.033 / −0.183 / +0.512 / −0.000 / +0.002） | 否 |

中止条件のうち **「source-KIF holdout で改善が消える / 符号が局ごとに反転する」**と
**「top-1 得点は上がらず、順位一致だけが上がる」**の2つに当たる。
bounded residual の macro が正に出る件も、**十分な状態数のある2局では横ばい**なので
「改善が少数の状態で説明される」に該当する。

**候補生成は律速ではない**（recall 99.7%）ので、issue が用意していた分岐
「candidate recall が低ければ次 issue を候補生成の多様化へ切り替える」は**該当しない**。

## 再判定に要るもの（issue の「追加4局」を具体化する）

方向自体（人手の局所順位に情報がある）は否定されていない。concordance が W に
単調なのは信号が実在する証拠で、`bin/eval_features` の回帰と向きが一致する係数も出た。
再判定するなら、**先に2つの構造問題を直す**。順番が逆だと同じ結果が出る。

1. **採点の被覆を、方策が到達しうる範囲まで広げる**（今回の主因）。
   候補 113.8 手のうち採点は 18.3 手（16.1%）で、現行 top-10 でも 75.7% しか
   採点されていない。順位を組み替える器を入れる以上、**その器が持ち上げる手が
   採点済みでなければ測れない**。既存の `append_unscored.py` ループ（候補ノブで
   suite → 2回以上選ばれた未収載手を `?` 追記 → 採点 → `sync_eval.py`）を、
   実験ごとでなく**学習ループの内側**へ入れる必要がある。
   概算コスト: 現行の被覆（上位15）を上位30へ広げるだけで
   1棋譜あたり +約9手/決定点 ×約100決定点 = **+900手の採点**
2. **独立棋譜を増やす**。採点の 94.4% が2局由来で、学習曲線の標準偏差は
   本数によらず ±0.21〜0.29。±0.05 の門をこの分散で判定できない。
   さらに macro 平均は1決定状態のクラスタと106状態のクラスタを同じ重みで扱うので、
   **小さい eval が増えるほど macro が実態から離れる**（今回の bounded residual が
   まさにその形で正に出た）。追加は**既存2局の近傍でなく対人実戦から**、
   先後・序中終盤・被王手・反則後で層化して**4局以上、かつ全局レビュー**
   （1決定点だけの eval を増やしても macro を歪めるだけ）

この2つが済むまで、人手採点は**目的関数（suite / SPSA）としてだけ使う**という
現状の役割を変えない。`bin/export_eval_rank_data` と `scripts/eval_rank/fit_residual.py`
は、採点が増えたときに同じ判定をやり直すための配管として残す（既定挙動は不変）。
